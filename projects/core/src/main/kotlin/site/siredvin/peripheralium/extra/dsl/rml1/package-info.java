/**
 * This package is dedicated for rl1 language.
 * RL1 is acronym to Render Markup Language v1.
 * Draft design is pretty simple:
 * t.y(10);r.z(15);t.y(0.1)
 * In general it looks like `lexeme(arguments);`
 */
package site.siredvin.peripheralium.extra.dsl.rml1;
